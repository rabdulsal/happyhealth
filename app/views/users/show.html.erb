<p id="notice"><%= notice %></p>

<h1>Your Lifecycle</h1>

<h2>See a realtime feed of all the latest updates regarding your personal health.</h2>

<div>
	<button class="submit-button right" title="Tooltip test">Update</button>
</div>

<div class="widget clear" id="one">
	<!-- New Title: Upcoming -->
	<h2>Upcoming Appointments</h2>
	<div class="dashAppointments">
		<% if @appointments != [] %>
			<% @appointments.each do |appointment| %>
				<%= render 'appointment', :appointment => appointment %>
	    	<% end %>
		<% else %>
			<i>You don't have any appointments scheduled this week.</i>
		<% end %>
	</div>
</div>

<!-- *****************************************

	COMMENTED OUT FOR BETA!

	******************************************* -->
	
<div class="widget" id="two">
	<!-- New Title: Previous -->
	<h2>Latest Notes</h2>
	<div class="dashAppointments">
		<% if @user.notes != []%>
			<% @user.notes[0..2].each do |note| %>
				<%= render 'note', :note => note%>
			<% end %>		
		<% else %>
			<i>You do not have any current notes. Make one now!</i>
		<% end %>
	</div>
</div>




