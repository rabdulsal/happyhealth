<!-- ************* FORM PARTIAL FOR EDITING A PREVIOUSLY CONSTRUCTED NOTE *************** -->

<%= form_for(@note, :url => {:controller => 'notes', :action => "update"}, :remote => true) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="note_field">
   	<%#= f.text_field :info %>
  </div>
  <div class="actions">
    <%#= f.submit %>
  </div> -->

  <div class="note-field margin-top clearfix">
    <div class="inline">
      <%= f.text_area :info, class: "two_third_width" %>
    </div>

    <%= f.submit "Post", class: "submit-button right quarter_width" %>
  </div>

<% end %>